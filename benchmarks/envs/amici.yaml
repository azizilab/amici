name: amici
channels:
  - conda-forge
dependencies:
  - python=3.11
  - pip
  - pip:
      - "-e ../../.."
      - pytest
      - pandas
      - scanpy
      - scikit-misc
      - igraph
      - leidenalg
